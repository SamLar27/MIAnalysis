% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IPD_one_stage.R
\name{IPD_one_stage}
\alias{IPD_one_stage}
\title{IPD One-Stage Modelling on Multiply Imputed Data (with RCS support + parallelization)}
\usage{
IPD_one_stage(
  data,
  outcome_var,
  predictor_vars,
  covariables = NULL,
  imp_col = ".imp",
  imp_n = NULL,
  model_type = "nb",
  followup_offset = "No",
  followup_col = NULL,
  trial_factor = "No",
  trial_col = NULL,
  time_col = NULL,
  event_col = NULL,
  formula_string = NULL,
  highlight_interactions = TRUE,
  spline_terms = NULL,
  spline_knots_n = NULL,
  spline_knots_percentile = NULL,
  random_intercept_var = NULL,
  predictor_vars_random_slope = NULL,
  covariables_random_slope = NULL,
  stratified_intercept_var = NULL,
  model_performance = FALSE,
  weighted_intercept = FALSE,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{data}{Data frame with all imputations stacked.}

\item{outcome_var}{Dependent variable (for non-Cox models).}

\item{predictor_vars}{Character vector of predictors to be included as fixed effects.}

\item{covariables}{Character vector of covariables (always included as fixed effects).}

\item{imp_col}{Column name indicating imputation index (default ".imp").}

\item{imp_n}{Number of imputations (if NULL, detected automatically).}

\item{model_type}{One of: "nb","lm","bin","poisson","gamma",
"quasipoisson","quasibinomial","cox".}

\item{followup_offset}{"Yes"/"No" – whether to include offset(log(followup_col)).}

\item{followup_col}{Name of follow-up duration column if offset used.}

\item{trial_factor}{"Yes"/"No" – include trial as additional fixed factor.}

\item{trial_col}{Trial column (if trial_factor = "Yes").}

\item{time_col}{Time variable for Cox models.}

\item{event_col}{Event variable for Cox models.}

\item{formula_string}{Optional custom formula (overrides automatic building).}

\item{highlight_interactions}{Logical, add flags for interactions/splines.}

\item{spline_terms}{Character vector of variable names for rcs() terms.}

\item{spline_knots_n}{Number of knots (if percentiles are not given).}

\item{spline_knots_percentile}{Numeric vector of percentiles for knots (0–100).}

\item{random_intercept_var}{Grouping variable for random effects (NULL = no random effects).}

\item{predictor_vars_random_slope}{Character vector of predictors with random slope.}

\item{covariables_random_slope}{Character vector of covariables with random slope.}

\item{stratified_intercept_var}{Variable defining stratified intercept (GLM) / baseline hazard (Cox).}

\item{model_performance}{Logical; if TRUE, compute model performance via \code{performance::model_performance()}.}

\item{weighted_intercept}{Logical; if TRUE and \code{stratified_intercept_var} is not NULL,
computes a single weighted intercept across strata.}

\item{parallel}{Logical; if TRUE, fit imputations in parallel.}

\item{n_cores}{Number of cores for parallel fitting. If NULL, defaults to detectCores() - 1.}
}
\value{
A list of class "IPD_one_stage" with elements:
\itemize{
\item table              : pooled fixed-effect estimates (without stratified intercept rows)
\item term               : same as table$term
\item Intercept          : intercept + stratified intercept rows
\item Weighted_intercept : one-row data.frame with weighted intercept (if requested)
and attributes:
\item "fit_log", "models", "performance_per_imp", "model_type", "formula",
"imputations", "n_imp", "has_random_effects", "random_intercept_var",
"stratified_intercept_var", "spline_info".
}
}
\description{
Fits models to multiply imputed datasets and pools results using Rubin's Rules.
Supports GLM, mixed models (via lme4 / glmmTMB / coxme), interactions, and
restricted cubic splines (rcs) via the \code{rms} package. Designed for IPD
one-stage analyses with optional random effects / stratified intercepts.
}
\details{
Intercept / slope structure is inferred automatically:
\itemize{
\item If \code{stratified_intercept_var} is provided:
\itemize{
\item GLM: adds \code{+ as.factor(stratified_intercept_var)} (keeps regular intercept)
\item Cox: adds \code{+ strata(stratified_intercept_var)}
}
\item If \code{random_intercept_var} is provided:
\itemize{
\item Mixed model is used (glmmTMB/lme4/coxme)
\item Random intercept and/or random slopes are defined by
\code{predictor_vars_random_slope} and \code{covariables_random_slope}.
Special case when \code{random_intercept_var == stratified_intercept_var}:
\itemize{
\item and no random slopes      -> no random effect (pure stratified intercept)
\item and at least one slope    -> random slopes only:
(0 + slopes | random_intercept_var)
}
}
}

Restricted cubic splines:
\itemize{
\item Use \code{spline_terms} (character vector of variable names).
\item Knots are defined ONCE on the full stacked data via:
\itemize{
\item \code{spline_knots_percentile}: numeric vector of percentiles (e.g. c(5,50,95)),
applied to the whole distribution of each variable, OR
\item \code{spline_knots_n}: number of knots; default percentiles are spread between 5 and 95.
}
\item Requires package \code{rms}; internally uses \code{rms::rcs(x, parms = knots)}.
\item Spline coefficients in the output are renamed as:
\verb{<var>_rcs_linear}, \verb{<var>_rcs_nl1}, \verb{<var>_rcs_nl2}, ...
}

Random slopes + splines:
\itemize{
\item If a variable appears in \code{spline_terms} and also in
\code{predictor_vars_random_slope} or \code{covariables_random_slope}, the raw
variable name is automatically expanded to its spline basis terms for
the random-effects part.
}

Parallelization:
\itemize{
\item If \code{parallel = TRUE}, model fitting across imputations is distributed over \code{n_cores}.
\item On Unix/macOS/Linux, uses \code{parallel::mclapply()}.
\item On Windows, uses \code{parallel::parLapply()} with a PSOCK cluster.
}
}
