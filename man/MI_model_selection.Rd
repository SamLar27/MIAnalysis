% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MI_model_selection.R
\name{MI_model_selection}
\alias{MI_model_selection}
\title{Model Selection for Multiple Imputed Data with Grouped Variables}
\usage{
MI_model_selection(
  data,
  outcome_var,
  core_vars_list,
  potential_vars_list = NULL,
  imp_col = ".imp",
  followup_offset = "No",
  followup_col = NULL,
  trial_factor = "No",
  trial_col = NULL,
  imp_n = NULL,
  model_type = "nb",
  random_intercept = "No",
  random_intercept_var = NULL,
  core_selection_criteria = "AICc",
  potential_selection_strategy = "forward",
  potential_selection_term = "p_value",
  potential_selection_threshold = 0.05,
  max_models = 1000,
  verbose = TRUE
)
}
\arguments{
\item{data}{A data frame containing the imputed dataset.}

\item{outcome_var}{The dependent variable for the model.}

\item{core_vars_list}{List of core variables or combinations to test for the base model.
Can be a list of character vectors, each representing a possible core model.}

\item{potential_vars_list}{Named list of additional covariables to test, grouped by category.
Variables within the same category are treated as alternatives (can't appear together).}

\item{imp_col}{The column name indicating imputation indices (default is ".imp").}

\item{followup_offset}{Whether to include an offset for follow-up duration ("Yes" or "No").}

\item{followup_col}{The column representing follow-up duration (required if followup_offset = "Yes").}

\item{trial_factor}{Whether to include trial as a factor ("Yes" or "No").}

\item{trial_col}{The column for trial factor (required if trial_factor = "Yes").}

\item{imp_n}{Number of imputations (if NULL, detected automatically).}

\item{model_type}{The model type: "nb", "poisson", "lm", or "bin"/"logistic".}

\item{random_intercept}{Whether to include a random intercept in the model ("Yes" or "No").}

\item{random_intercept_var}{The grouping variable for the random intercept (required if random_intercept = "Yes").}

\item{core_selection_criteria}{Criteria for selecting core model: "AIC", "AICc", "BIC", "BICc", or "LRT".}

\item{potential_selection_strategy}{Strategy for additional covariable selection: "forward", "backward", or "brute_force".}

\item{potential_selection_term}{Term to evaluate for covariable selection: "p_value", "AIC", "AICc", "BIC", or "BICc".}

\item{potential_selection_threshold}{Threshold for covariable selection (e.g., 0.05 for p-value).}

\item{max_models}{Maximum number of models to evaluate (to prevent computational explosion in brute force).}

\item{verbose}{Whether to display progress information (default: TRUE).}
}
\value{
A list containing:
\item{best_model}{The final selected model output}
\item{core_model_selection}{Results from core model selection}
\item{covariable_selection}{Results from additional covariable selection}
\item{model_comparison}{Full comparison of tested models}
\item{final_formula}{Formula of the final selected model}
}
\description{
This function performs systematic model selection on multiply imputed data.
It handles both core model selection and additional covariable selection
through forward, backward, or brute force approaches. The potential variables
can be grouped into categories, where variables in the same category are considered
alternatives and cannot be included together in the model.
}
