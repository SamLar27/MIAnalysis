% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/IPD_two_stage.R
\name{IPD_two_stage}
\alias{IPD_two_stage}
\title{Two-stage IPD meta-analysis with multiple imputation and forest plot}
\usage{
IPD_two_stage(
  data,
  outcome_var,
  predictor_vars,
  covariables = NULL,
  imp_col = ".imp",
  followup_offset = c("Yes", "No"),
  followup_col = NULL,
  intercept_var,
  model_type = c("nb", "poisson", "quasipoisson", "gaussian", "binomial"),
  model_performance = FALSE,
  id_col = NULL,
  main_term = NULL,
  meta_method = "REML",
  xlab = "aRR",
  ref_line = 1,
  x_limits = NULL,
  x_breaks = NULL,
  center_axis_at_one = TRUE,
  point_size_range = c(2, 6),
  show_size_legend = FALSE,
  show_headers = TRUE,
  diamond_color = "blue",
  pooled_color = "black",
  bold_pooled = FALSE,
  margin_plot = c(5.5, 2, 5.5, 5.5),
  show_heterogeneity_in_forest = TRUE,
  het_x = 0.08,
  het_y = 0.085,
  het_line_gap = 0.028,
  het_text_size = 9
)
}
\description{
KEY MODIFICATION (N counting):
\itemize{
\item N PER TRIAL is computed from the ACTUAL DATA USED BY THE MODEL
after glm / glm.nb drops rows with missing data.
\item If id_col is provided, N is counted as DISTINCT SUBJECTS USED
(not number of rows).
\item IMPORTANT CHANGE REQUESTED HERE:
\itemize{
\item N is computed using ONLY THE FIRST IMPUTATION (imp1 / minimum .imp),
and there is NO n_count_imp argument anymore.
}
\item Forest pooled N = sum of the trial Ns (imp1) that contributed to the pooled estimate.
\item Table N_total uses the same logic, so it matches the pooled row in the forest plot.
}
}
